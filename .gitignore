node_modules/
dist/
.yarn/*
!.yarn/patches
!.yarn/releases
.yarn/install-state.gz
yarn-error.log
*.tsbuildinfo
.DS_Store

# Log des lokalen Entwicklungs-Webservers (dev/serve.mjs)
dev/serve.log

# Bytecode der Hilfsskripte
__pycache__/
*.pyc

# Das Kartenbündel wird gebaut und beim Release beigelegt
custom_components/family_tracking/www/

# Local development helpers: the Home Assistant test container, the deploy
# script, the tile prober. None of them is needed to install this or to build a
# release, and they only add noise for anyone reading the repository. They stay
# on disk for whoever set them up.
.devcontainer/
dev/
scripts/
